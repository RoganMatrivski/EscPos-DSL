[package]
name = "escpos_dsl"
version = "0.1.3"
edition = "2024"

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
boltffi = "0.29.2"
escpos = { version = "0.19.0", features = ["graphics"] }
image = "0.25"
base64 = "0.22"
winnow = "0.7"

[package.metadata.release]
publish = false
tag-name = "v{{version}}"

